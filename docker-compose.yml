services:
  db:
    container_name: vvv-db
    image: postgres:16-alpine
    ports:   
      - "5433:5432"
    env_file: .env
    

  adminer:
    container_name: vvv-adminer
    image: adminer:latest
    ports:
      - "8080:8080"
    depends_on:
      - db
